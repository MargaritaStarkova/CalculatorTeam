<resources>

    <style name="Theme.MainActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/appColor</item>
        <item name="android:statusBarColor">@color/statusBar</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize neomorph theme here. -->
        <item name="neomorph_frame">@drawable/neomorph_day_image</item>
        <item name="neomorph_btn">@drawable/neomorph_day_btn</item>
        <item name="neomorph_background">@drawable/container</item>
        <item name="neomorph_background_text">@drawable/custom_label</item>
        <item name="neomorph_color_text">@color/black</item>
    </style>
    <style name="Theme.ComposeBlackWhiteActivity" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>
</resources>

